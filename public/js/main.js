/**
 * Global vanilla JS entry point.
 * Phase-specific interactions (form validation, PDF download
 * triggers, admin panel widgets) will be added as separate,
 * modular files and imported per-page rather than piled in here.
 */
'use strict';

document.addEventListener('DOMContentLoaded', () => {
  // Intentionally empty at Phase 0.
});
